import styled from 'styled-components';

const Image = styled.img`
  max-width: 250px;
  max-height: 250px;
  margin: 0 auto 1rem;
  @media (min-width: 760px) {
    margin: 0 1rem 0 0;
  }
`;

export default Image;
