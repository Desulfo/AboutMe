import styled from 'styled-components';

const Form = styled.form`
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  padding: 0.5rem 0;
  max-width: 800px;
  margin: 0 auto;
`;

export default Form;
